format = """
[](#f38ba8)\
$os\
$username\
[](bg:#a6e3a1 fg:#f38ba8)\
$directory\
[](fg:#a6e3a1 bg:#cdd6f4)\
$git_branch\
$git_status\
[](fg:#cdd6f4 bg:#eba0ac)\
$c\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$rust\
$scala\
[](fg:#eba0ac bg:#fab387)\
$docker_context\
[](fg:#fab387 bg:#f9e2af)\
$time\
[ ](fg:#f9e2af)\
"""

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bg:#f38ba8 fg:#1e1e2e bold"
style_root = "bg:#cdd6f4 fg:#1e1e2e bold"
format = '[  / $user ]($style)' # Added more padding around the Arch logo
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bg:#b4befe fg:#11111b"
disabled = true # Disabled by default

[directory]
style = "bg:#a6e3a1 fg:#11111b"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[c]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:#a6adc8 fg:#1e1e2e"
format = '[ $symbol $context ]($style)'

[elixir]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:#cdd6f4 fg:#1e1e2e"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#cdd6f4 fg:#1e1e2e"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:#bac2de fg:#1e1e2e"
format = '[ $symbol ($version) ]($style)'

[time]
disabled = false
time_format = "%R" # Hour:Minute Format
style = "bg:#f9e2af fg:#1e1e2e"
format = '[  $time ]($style)'

